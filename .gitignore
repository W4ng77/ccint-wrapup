# LaTeX 编译产物
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.lof
*.lot
main.pdf
tldr.pdf

# figures/*.pdf 是 `ccint viz` 的产物，但**必须提交** ——
# Overleaf 侧没有数据库，拉不到图就编译不出来。

# 不要同步进 Overleaf：
# 这两个文件各自含有完整的 \documentclass，Overleaf 会认成第二个主文档
ccint-wrapup-standalone.tex
ccint-overleaf.zip

# 图的 TikZ 源码带自己的 \documentclass，放在 figures/src/ 下
# 以免 Overleaf 把它当成第二个主文档
architecture.pdf
